<header class="site-header" role="banner" id="site-header">
  <div class="header-container page-width">
    <!-- Menu Toggle & Search (Left) -->
    <div class="header__left">
      <button type="button" class="header__menu-toggle js-menu-toggle" aria-controls="MobileNav" aria-expanded="false" aria-label="Toggle menu">
        {% render 'icon-menu' %}
        {% render 'icon-close' %}
        <span class="desktop-only header__menu-toggle-label">Menu</span>
      </button>
      
      <button type="button" class="header__icon header__icon--search focus-inset js-search-toggle" aria-label="Search">
        {% render 'icon-search' %}
        <span class="desktop-only header__icon-label">Search</span>
      </button>
    </div>

    <!-- Brand Logo (Center) -->
    <div class="header__center">
      <div class="header__logo">
        <a href="{{ routes.root_url }}" class="header__logo-link">
          {%- if section.settings.logo != blank -%}
            {%- assign logo_alt = section.settings.logo.alt | default: shop.name | escape -%}
            {{ section.settings.logo | image_url: width: 250 | image_tag: class: 'header__logo-img', alt: logo_alt, preload: true }}
          {%- else -%}
            <img src="{{ 'logo.png' | asset_url }}" alt="{{ shop.name | escape }}" class="header__logo-img" width="250" height="250" style="width: auto; height: auto;" preload="true">
          {%- endif -%}
        </a>
      </div>
    </div>

    <!-- Actions (Right) -->
    <div class="header__right">
      <div class="header__icons">
        {%- if shop.customer_accounts_enabled or true -%}
          <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}" class="header__icon header__icon--account" aria-label="Account">
            <svg viewBox="0 0 20 20" class="icon icon-user" fill="none" stroke="currentColor" stroke-width="1.5" style="width: 22px; height: 22px;"><path d="M10 11a4 4 0 100-8 4 4 0 000 8zM3 18a7 7 0 0114 0" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </a>
        {%- endif -%}

        {%- if section.settings.whatsapp_number != blank -%}
          <a href="https://wa.me/{{ section.settings.whatsapp_number }}" class="header__icon header__icon--whatsapp desktop-only" target="_blank" rel="noopener" aria-label="WhatsApp">
            <svg viewBox="0 0 24 24" style="width: 22px; height: 22px;" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
          </a>
        {%- endif -%}

        <a href="{{ routes.cart_url }}" class="header__icon header__icon--cart focus-inset js-cart-toggle" id="cart-icon-bubble" aria-label="Cart">
          {% render 'icon-cart' %}
          <div class="cart-count-bubble {% if cart.item_count == 0 and false %}hidden{% endif %}">
            <span aria-hidden="true" id="cart-count-number">{{ cart.item_count }}</span>
            <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
          </div>
        </a>
      </div>
    </div>
  </div>

  <!-- Navigation Drawer (Left Slide-out) -->
  <div id="MobileNav" class="navigation-drawer" aria-hidden="true">
    <nav class="navigation-drawer__nav" role="navigation">
      <ul class="navigation-drawer__list" role="list">
        {%- for link in section.settings.menu.links -%}
          <li class="navigation-drawer__item">
            {%- if link.links != blank -%}
              <details class="navigation-drawer__details">
                <summary class="navigation-drawer__summary">
                  {{ link.title | escape }}
                  <span class="icon-arrow">
                    <svg viewBox="0 0 10 6" class="icon icon-chevron-down" fill="none" stroke="currentColor" stroke-width="1.5" style="width: 10px; height: 6px;"><path d="M1 1l4 4 4-4" stroke-linecap="round" stroke-linejoin="round"/></svg>
                  </span>
                </summary>
                <ul class="navigation-drawer__submenu" role="list">
                  {%- for childlink in link.links -%}
                    <li>
                      <a href="{{ childlink.url }}" class="navigation-drawer__sublink">
                        {{ childlink.title | escape }}
                      </a>
                    </li>
                  {%- endfor -%}
                </ul>
              </details>
            {%- else -%}
              <a href="{{ link.url }}" class="navigation-drawer__link">
                {{ link.title | escape }}
              </a>
            {%- endif -%}
          </li>
        {%- endfor -%}
      </ul>
    </nav>
  </div>

  <!-- Search Modal -->
  <div class="search-modal" aria-hidden="true">
    <div class="search-modal__content">
      <form action="{{ routes.search_url }}" method="get" role="search" class="search-form">
        <div class="search-form__field">
          <input class="search-form__input"
            id="Search-In-Modal"
            type="search"
            name="q"
            value="{{ search.terms | escape }}"
            placeholder="{{ 'general.search.placeholder' | t }}"
          >
          <input type="hidden" name="type" value="product">
          <button class="search-form__submit" aria-label="{{ 'general.search.submit' | t }}">
            {% render 'icon-search' %}
          </button>
        </div>
      </form>
      <button type="button" class="search-modal__close js-search-close" aria-label="Close search">
        {% render 'icon-close' %}
      </button>
    </div>
  </div>
</header>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo Image"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "main-menu"
    },
    {
      "type": "text",
      "id": "whatsapp_number",
      "label": "WhatsApp Number (with country code, e.g. 353851234567)",
      "info": "Leave blank to hide WhatsApp icon"
    }
  ]
}
{% endschema %}
